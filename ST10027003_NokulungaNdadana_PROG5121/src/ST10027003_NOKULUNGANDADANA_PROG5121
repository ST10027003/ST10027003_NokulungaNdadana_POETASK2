

/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */

/**
 *
 * @author Nokulunga Ndadana
 */
public class NewClass {
    int v = 0;
   String taskName;
   int taskNumber;
   String taskDescription;
   String developerDetails;
   int taskDuration;
   String taskID;
   String taskStatus;
    
    Scanner nokulunga = new Scanner (System.in);
    
    public void setTaskName (String taskName) {
        nokulunga.taskName = taskName;
    }
    public String getTaskName() {
        return nokulunga.taskName;
    }
    public void setTaskNumber(int taskNumber) {
        nokulunga.taskNumber = taskNumber;
    }
    public int getTaskNumber() {
        return nokulunga.taskNumber;
    }
    public void setTaskDescription (String taskDescription) {
        nokulunga.taskDescription = taskDescription;    
    }
    public String getTaskDescription() {
        return nokulunga.taskDescription;
    }
    public void setDeveloperDetails (String developerDetails) {
        nokulunga.developerDetails = developerDetails;
    }
    public String getDeveloperDetails () {
        return nokulunga.developerDetails;
    }
    public void setTaskDuration (int taskDuration) {
        nokulunga.taskDuration = taskDuration;
    }
    public int getTaskDuration () {
        return nokulunga.taskDuration;
    }
    public void setTaskID (String taskID) {
        nokulunga.taskID = taskID;
    }
    public String getTaskID() {
        return nokulunga.taskID;
    }
    public void setTaskStatus(String taskStatus) {
        nokulunga.taskStatus = taskStatus;
    }
    public String getTaskStatus() {
        return nokulunga.taskStatus;
    }
    
    public boolean checkTaskDescription (boolean checkTaskDescription) {
        for (int n=0; n<taskDescription.length(); n++ ) {
            if (taskDescription.length() <=50)
                return true;
        }
        return false;
            
        }
  
    public String createTaskID (String taskName, String taskNumber, String developer) {
        int taskNumber = 1 + i;
       
          StringBuilder taskIDnn = new StringBuilder();
          taskIDnn.append(Login.firstTwoTaskName);
          taskIDnn.append (":");
          taskIDnn(Login,taskNumber);
          taskIDnn.append (":");
          taskIDnn.append (Login.lastThreeDev);
          String taskID = taskIDnn.toString();
          finalTaskID = task.toUppercase();
          
          String taskID = taskName.substring(0,2) + ":" + taskNumber + ":" taskNumber + ":" + name.substring(name,length()- 3).toUppercase();
          return finalTaskID;

}
    
    public int returnTotalHours (int taskDuration) {
        return = 0;
    
        int totalNumberOfHours = totalNumberOfHours + taskDuration;
        for (task tsk : tasks)
            }
        
}
    
    
    public String printTaskDetails (String taskDetails) {
      String taskDetails = ("Task details\ntask status :"
              
              return taskDetails;
  }
